const menu=document.querySelector('#menu'),nav=document.querySelector('.site-header nav');menu?.addEventListener('click',()=>nav.classList.toggle('open'));document.querySelectorAll('.site-header nav a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));const io=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting)e.target.classList.add('in')}),{threshold:.14});document.querySelectorAll('.reveal').forEach(el=>io.observe(el));const counter=document.querySelector('.counter');let counted=false;const cio=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting&&!counted){counted=true;let n=0,t=Number(counter.dataset.target||100);const tick=()=>{n+=2;counter.textContent=Math.min(n,t);if(n<t)requestAnimationFrame(tick)};tick()}}),{threshold:.6});if(counter)cio.observe(counter);window.addEventListener('scroll',()=>{const p=document.querySelector('.parallax');if(p&&window.innerWidth>900){const r=p.getBoundingClientRect();p.style.backgroundPosition=`center ${50+r.top*.018}%`}});document.querySelector('#contactForm')?.addEventListener('submit',e=>{e.preventDefault();document.querySelector('#status').textContent='Thanks — the interface is working. Connect a verified production form endpoint before launch.'});